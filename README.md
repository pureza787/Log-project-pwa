# 📚 Student Task Organizer (ระบบช่วยจัดการงานและการบ้าน)

**Student Task Organizer** เป็นเว็บแอปพลิเคชัน (Web Application) ที่พัฒนาขึ้นเพื่อช่วยให้นักเรียนและนักศึกษาบริหารจัดการตารางงาน การบ้าน และการสอบได้อย่างมีประสิทธิภาพ ช่วยให้ไม่พลาดทุกกำหนดการสำคัญ (Deadline) มาพร้อมกับดีไซน์ที่ทันสมัย ใช้งานง่าย และรองรับการทำงานแบบ PWA (Progressive Web App)

![Project Preview](./logo.png)

## ✨ คุณสมบัติเด่น (Key Features)

* **📝 ระบบจัดการงาน (Task Management):** สามารถเพิ่ม, แก้ไข, ลบรายการงาน และบันทึกสถานะเมื่อทำเสร็จแล้วได้อย่างสะดวก
* **🔔 แจ้งเตือนอัจฉริยะ (Smart Notifications):** มีระบบแจ้งเตือนเมื่อใกล้ถึงกำหนดส่ง (ส่งวันนี้, พรุ่งนี้, อีก 3 วัน) หรือเมื่อเลยกำหนดส่ง เพื่อให้ผู้ใช้รับทราบทันที
* **📊 แดชบอร์ดสรุปผล (Dashboard):** แสดงภาพรวมสถิติงานทั้งหมด งานที่เสร็จแล้ว งานค้างส่ง และงานเร่งด่วน ในรูปแบบที่เข้าใจง่าย
* **📱 รองรับ PWA (Offline Support):**
    * สามารถติดตั้งลงบนหน้าจอหลัก (Add to Home Screen) ของมือถือหรือคอมพิวเตอร์ได้เหมือนแอปพลิเคชันทั่วไป
    * รองรับการใช้งานแม้อินเทอร์เน็ตไม่เสถียรด้วยเทคโนโลยี Service Worker
* **☁️ Cloud Sync (Firebase):** เชื่อมต่อฐานข้อมูลจริงผ่าน **Firebase Firestore** ทำให้ข้อมูลมีความปลอดภัยและอัปเดตแบบ Realtime
* **🎨 User Interface:** ออกแบบโดยเน้นความสะอาดตา (Clean Design) แยกความสำคัญของงานด้วยสี (Color Coded) และรองรับการแสดงผลทุกขนาดหน้าจอ (Responsive)

## 🛠 เทคโนโลยีที่ใช้ (Tech Stack)

* **Frontend:** HTML5, CSS3, JavaScript (ES6 Modules)
* **Database:** Firebase Firestore
* **PWA Features:** Service Worker (`sw.js`), Web App Manifest
* **Tools:** VS Code, Live Server

## 📂 โครงสร้างโปรเจกต์

* `index.html`: ส่วนแสดงผลหลักของแอปพลิเคชัน
* `styles.css`: ไฟล์กำหนดรูปแบบและการตกแต่ง (Styling)
* `script.js`: ส่วนประมวลผลหลัก เชื่อมต่อฐานข้อมูลและจัดการตรรกะของโปรแกรม
* `sw.js`: Service Worker สำหรับจัดการ Cache และ Offline Mode
* `manifest.json`: ไฟล์ตั้งค่าสำหรับ Web App Install

## 🚀 วิธีการติดตั้งและใช้งาน (Installation)

เนื่องจากโปรเจกต์นี้ใช้งาน ES Modules จำเป็นต้องรันผ่าน Local Server ดังนี้:

1.  ดาวน์โหลดซอร์สโค้ดลงในเครื่องคอมพิวเตอร์
2.  เปิดโฟลเดอร์โปรเจกต์ด้วยโปรแกรม **VS Code**
3.  ติดตั้ง Extension ชื่อ **"Live Server"**
4.  คลิกขวาที่ไฟล์ `index.html` เลือกเมนู **"Open with Live Server"**

## ⚠️ หมายเหตุการพัฒนา (Developer Notes)

* **Firebase Setup:** โค้ดปัจจุบันมีการตั้งค่าเชื่อมต่อ Firebase ไว้อยู่แล้ว หากต้องการนำไปใช้ส่วนตัว แนะนำให้เปลี่ยน `firebaseConfig` ในไฟล์ `script.js` เป็นของโปรเจกต์ท่านเอง
* **Cache Update:** หากมีการแก้ไขโค้ด (CSS/JS) แล้วหน้าเว็บไม่อัปเดต ให้ทำการเปลี่ยนชื่อเวอร์ชัน Cache ในไฟล์ `sw.js` (เช่น จาก `v4` เป็น `v5`) เพื่อบังคับให้บราวเซอร์โหลดไฟล์ใหม่

## 🤝 การสนับสนุน

หากสนใจนำโปรเจกต์นี้ไปพัฒนาต่อ สามารถ Fork หรือดาวน์โหลดไปศึกษาได้เลยครับ
